import 'package:flutter/material.dart';

class AmbientNatureSound extends StatelessWidget {
  const AmbientNatureSound({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
