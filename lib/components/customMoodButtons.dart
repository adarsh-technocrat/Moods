import 'package:flutter/material.dart';

class CustomMoodButtons extends StatelessWidget {
  const CustomMoodButtons({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
